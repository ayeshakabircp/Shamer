kind = "web"
previewPath = "/"
title = "Shamer"
version = "1.0.0"
id = "artifacts/shamer"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25059

[services.development]
run = "pnpm --filter @workspace/shamer run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/shamer", "run", "build" ]
publicDir = "artifacts/shamer/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25059"
BASE_PATH = "/"
